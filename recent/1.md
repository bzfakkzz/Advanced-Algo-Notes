去重：

```cpp
sort(a.begin(),a.end());
a.earse(unique(a.begin(),a.end()),a.end());
```



删除指定元素：

```cpp
st.erase(3);  // 删除值为3的元素
```

```cpp
auto it = remove(a.begin(),a.end(),2); // 删除值为2的元素
a.erase(it, a.end());
```

```cpp
mp.earse(20); // 删除key为20的元素
```



同模取余：

每k个为一组，轮到第idx个

```cpp
dist[u]%k==idx%k;
```



避免超时：

```cpp
ios::sync_with_stdio(false);
cin.tie(0);
cout.tie(0);
```



dfs函数尽量使用局部变量，同一层级迭代同时进行数值可能混
